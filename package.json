{
  "name": "taskflow-root",
  "private": true,
  "scripts": {
    "build": "npm --prefix server install --build-from-source && npm --prefix client install && npm --prefix client run build",
    "start": "npm --prefix server start"
  }
}
